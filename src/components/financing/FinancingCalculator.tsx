import { useLoanCalculator } from '../../hooks/useLoanCalculator';
import { LoanResult } from './LoanResult';
const parseNumber = (value: string): number => Number(value) || 0;
export const FinancingCalculator = () => {
  const calculator = useLoanCalculator();
  return <section className="calculator-section" id="financing"><div className="calc-container"><div className="calc-info"><h2>Auto Loan Estimator</h2><p>Estimate your monthly repayments based on current bank financing plans in Kenya. Choose your budget terms below.</p></div><div className="calc-form"><div className="form-group"><label htmlFor="calcPrice">Vehicle Price (KSh)</label><input id="calcPrice" type="number" value={calculator.price} step="100000" onChange={(event) => calculator.setPrice(parseNumber(event.target.value))} /></div><div className="form-group"><label htmlFor="calcDeposit">Deposit / Down Payment (KSh)</label><input id="calcDeposit" type="number" value={calculator.deposit} step="50000" onChange={(event) => calculator.setDeposit(parseNumber(event.target.value))} /></div><div className="form-group"><label htmlFor="calcTerm">Loan Duration</label><select id="calcTerm" value={calculator.months} onChange={(event) => calculator.setMonths(parseNumber(event.target.value))}><option value="12">12 Months (1 Year)</option><option value="24">24 Months (2 Years)</option><option value="36">36 Months (3 Years)</option><option value="48">48 Months (4 Years)</option><option value="60">60 Months (5 Years)</option></select></div><div className="form-group"><label htmlFor="calcInterest">Estimated Interest Rate (% p.a.)</label><input id="calcInterest" type="number" value={calculator.annualRate} step="0.5" onChange={(event) => calculator.setAnnualRate(parseNumber(event.target.value))} /></div><LoanResult monthlyPayment={calculator.monthlyPayment} /></div></div></section>;
};
